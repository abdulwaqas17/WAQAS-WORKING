// --------give a username --------
let fullName = prompt('Enter your full name, and get Username');
// let username = alert('@'+fullName+fullName.length);
let username = alert(`@${fullName}${fullName.length}`);